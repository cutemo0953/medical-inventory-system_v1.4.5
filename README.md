# 醫療站庫存管理系統 v1.4.5

> 為壯闊台灣醫療站、備援手術站(BORP)與物資中心設計的簡易庫存管理系統

---

## 📋 目錄

1. [系統簡介](#系統簡介)
2. [給護理師的安裝指南](#給護理師的安裝指南)
3. [功能介紹](#功能介紹)
4. [使用說明](#使用說明)
5. [常見問題](#常見問題)
6. [技術資訊](#技術資訊)

---

## 系統簡介

這是一套專為**醫療站**設計的庫存管理系統，就像是「數位化的物資登記簿」，可以幫助護理師快速記錄和查詢：

- ✅ 藥品庫存（止痛藥、抗生素等）
- ✅ 血袋數量（A+, O-, AB+ 等各種血型）
- ✅ 設備狀態（電源站、淨水器、手術包等）
- ✅ 手術記錄與耗材消耗

### v1.4.5 新功能

- **多站點切換**：可以在不同醫療站之間切換（TC-01、BORP-01、LOG-HUB）
- **自動複製範本**：新建站點時自動載入設備清單，不用手動輸入
- **手術包管理**：清楚區分一般設備與手術器械
- **藥品領用系統**：支援正常領用與緊急領用（Break-the-Glass）

---

## 給護理師的安裝指南

> ⚠️ **第一次使用電腦系統嗎？沒關係！跟著以下步驟一步步做就好。**

### 步驟一：檢查電腦是否已安裝 Python

#### Windows 電腦：

1. 按鍵盤上的 **Windows鍵 + R**
2. 輸入 `cmd` 然後按 **Enter**
3. 出現黑色視窗後，輸入：
   ```
   python --version
   ```
4. 如果看到類似 `Python 3.11.5` 的字樣 → **已安裝，跳到步驟二**
5. 如果出現錯誤 → **需要安裝 Python**

**安裝 Python（如果需要）：**
1. 前往：https://www.python.org/downloads/
2. 下載最新版本（點擊黃色的 Download Python 按鈕）
3. 執行下載的檔案
4. ⚠️ **重要！勾選「Add Python to PATH」**
5. 點擊「Install Now」
6. 等待安裝完成

#### Mac 電腦：

1. 按 **Command + 空白鍵** 打開 Spotlight
2. 輸入 `terminal` 然後按 **Enter**
3. 出現黑色/白色視窗後，輸入：
   ```
   python3 --version
   ```
4. 如果看到版本號碼 → **已安裝，跳到步驟二**
5. 如果需要安裝，系統會自動提示

---

### 步驟二：下載系統檔案

1. 從 GitHub 下載系統：
   - 前往：https://github.com/cutemo0953/medical-inventory-system_v1.4.5
   - 點擊綠色的 **Code** 按鈕
   - 選擇 **Download ZIP**

2. 解壓縮下載的 ZIP 檔案
   - Windows：對著檔案點右鍵 → 選擇「解壓縮全部」
   - Mac：直接雙擊 ZIP 檔案

3. 記住解壓縮後的資料夾位置（例如：桌面、下載資料夾）

---

### 步驟三：啟動系統

#### Windows 電腦：

1. 按 **Windows鍵 + R**
2. 輸入 `cmd` 然後按 **Enter**
3. 移動到系統資料夾（假設放在桌面）：
   ```
   cd Desktop\medical-inventory-system_v1.4.5
   ```
4. 安裝必要套件（**只需要做一次**）：
   ```
   pip install -r requirements_v1.4.5.txt
   ```
   等待安裝完成（可能需要 1-2 分鐘）

5. 啟動系統：
   ```
   python main.py
   ```

6. 看到類似下面的訊息就成功了：
   ```
   🏥 醫療站庫存管理系統 API v1.4.5
   🌐 服務位址: http://0.0.0.0:8000
   ```

#### Mac 電腦：

1. 打開 **Terminal**（終端機）
2. 移動到系統資料夾：
   ```
   cd ~/Downloads/medical-inventory-system_v1.4.5
   ```
3. 安裝必要套件（**只需要做一次**）：
   ```
   pip3 install -r requirements_v1.4.5.txt
   ```

4. 啟動系統：
   ```
   python3 main.py
   ```

---

### 步驟四：打開系統網頁

1. 啟動系統後，**不要關閉黑色視窗**（這是系統運作的地方）

2. 打開瀏覽器（Chrome、Safari、Edge 都可以）

3. 在網址列輸入：
   ```
   http://localhost:8000
   ```

4. 成功！你應該會看到系統首頁

---

### 每次使用的步驟

以後每次要使用系統，只需要：

1. 打開命令提示字元 / Terminal
2. 移動到系統資料夾：`cd Desktop/medical-inventory-system_v1.4.5`
3. 啟動系統：`python main.py`（Mac 用 `python3 main.py`）
4. 打開瀏覽器，輸入：`http://localhost:8000`

---

## 功能介紹

### 1. 📦 物品管理

**可以做什麼：**
- 查詢目前有哪些藥品/耗材
- 看每種物品還剩多少
- 記錄進貨（收到新物資時）
- 登記消耗（用掉物資時）

**操作位置：**
點擊上方選單的「庫存查詢」標籤

---

### 2. 🩸 血庫管理

**可以做什麼：**
- 查看各血型庫存（A+, A-, B+, B-, O+, O-, AB+, AB-）
- 記錄血袋入庫
- 登記血袋領用
- 查詢歷史紀錄

**特色功能：**
- **緊急血袋登記**：可以快速登記現場捐血的血袋
- **病患資訊記錄**：可以記錄是哪位病患使用了血袋

**操作位置：**
點擊上方選單的「血庫管理」標籤

---

### 3. 🔧 設備管理

**可以做什麼：**
- 檢查設備狀態（正常/警告/故障/待檢查）
- 記錄電力設備的電量百分比
- 管理手術包清單
- 每日自動重置檢查狀態（早上7點）

**設備分類：**
- **一般設備**：行動電源站、光觸媒、淨水器、行動冰箱
- **手術包**：骨科包、ASSET包、氣切輔助包、縫合包等

**操作位置：**
點擊上方選單的「設備管理」標籤

---

### 4. 📝 處置記錄

包含三種功能：

#### 手術記錄
- 記錄手術日期、病患、主刀醫師
- 登記手術耗材使用量
- 查詢歷史手術記錄

#### 一般消耗
- 快速登記物資消耗
- 可一次登記多項物品
- 自動扣除庫存

#### 藥品領用
- **正常領用**：需要藥師PIN碼審核
- **緊急領用**：可跳過審核，但需填寫緊急原因（供事後稽核）

**操作位置：**
點擊上方選單的「處置記錄」標籤

---

### 5. 🔄 站點切換（v1.4.5 新功能）

**可以切換的站點類型：**

| 站點類型 | 代碼範例 | 說明 |
|---------|---------|------|
| 醫療站 | TC-01, TC-02 | 一般醫療物資管理 |
| 備援手術站 | BORP-01, BORP-02 | 包含手術包與器械 |
| 物資中心 | LOG-HUB | 大量物資儲存與調配 |

**如何切換站點：**
1. 看右上角的「站點」下拉選單
2. 選擇要切換的站點（例如：BORP-01）
3. 系統會自動載入該站點的資料

**新增自訂站點：**
1. 點擊「其他站點...」
2. 選擇站點類型（TC/BORP/LOG）
3. 輸入編號（例如：03）
4. 按 ✓ 確認
5. 系統會詢問是否複製範本設備 → 建議選「確定」

---

## 使用說明

### 情境一：記錄進貨

**例如：收到 100 顆 Paracetamol (普拿疼)**

1. 點擊「庫存查詢」標籤
2. 點擊右上角「進貨登記」按鈕
3. 填寫資料：
   - 物品代碼：輸入 `MED-001` 或搜尋「Paracetamol」
   - 數量：`100`
   - 批號：（選填）例如 `LOT20240115`
   - 效期：（選填）例如 `2025-12-31`
4. 點擊「確認進貨」
5. 完成！庫存會自動增加

---

### 情境二：領用血袋

**例如：病患需要 2 袋 O+ 血**

1. 點擊「血庫管理」標籤
2. 在 O+ 血型那一列，點擊「領用」按鈕
3. 填寫資料：
   - 領用數量：`2`
   - 病患姓名：（選填）例如「張小明」
   - 病患編號：（選填）掃描 Triage Tag
4. 點擊「確認領用」
5. 完成！O+ 庫存會減少 2 袋

---

### 情境三：檢查設備

**例如：每天早上檢查行動電源站**

1. 點擊「設備管理」標籤
2. 找到「行動電源站」那一列
3. 點擊「檢查」按鈕
4. 選擇狀態：
   - **正常**：設備運作正常
   - **警告**：電力低於 20%
   - **故障**：無法使用
5. 填寫電力百分比：例如 `75`
6. 點擊「確認」
7. 完成！設備狀態已更新

---

### 情境四：緊急領藥

**例如：大量傷患湧入，需要立即領用 Morphine**

1. 點擊「處置記錄」標籤
2. 選擇「藥品領用」模式
3. 選擇藥品：`Morphine`
4. 輸入數量：例如 `5`
5. 選擇領藥人：例如「護理師 A」
6. **點擊紅色「🚨 緊急領用」按鈕**（不是「送出審核」）
7. 輸入緊急原因：例如「大量傷患湧入」
8. 點擊「確定」
9. 完成！庫存立即扣除，藥師可稍後確認

---

## 常見問題

### Q1: 啟動系統時出現「找不到 Python」？

**解決方法：**
1. 確認 Python 已安裝：輸入 `python --version`
2. 如果沒有，請回到「步驟一」重新安裝
3. Windows 用戶：安裝時務必勾選「Add Python to PATH」

---

### Q2: 網頁打不開，顯示「無法連線」？

**可能原因與解決方法：**

1. **系統沒有啟動**
   - 檢查黑色視窗是否還在運作
   - 如果關閉了，重新執行 `python main.py`

2. **埠號被佔用**
   - 換一個埠號啟動：`python main.py --port 8001`
   - 然後開啟：`http://localhost:8001`

3. **防火牆阻擋**
   - Windows：允許 Python 通過防火牆
   - Mac：系統偏好設定 → 安全性與隱私 → 允許

---

### Q3: 資料會不會不見？

**資料儲存機制：**
- 所有資料儲存在 `medical_inventory.db` 檔案中
- 只要這個檔案還在，資料就不會消失
- 建議每週備份這個檔案到 USB 隨身碟

**備份方法：**
1. 關閉系統（按 Ctrl+C）
2. 複製 `medical_inventory.db` 到安全的地方
3. 檔名可改成：`備份_20240115.db`

---

### Q4: 可以多台電腦同時使用嗎？

**v1.4.5 單站版的限制：**
- 同一時間只能一台電腦執行系統
- 其他電腦可透過網路連線查看（需在同一個 WiFi）

**連線方法：**
1. 在執行系統的電腦上查詢 IP 位址：
   - Windows：`ipconfig`
   - Mac：`ifconfig | grep inet`
2. 其他電腦開啟瀏覽器，輸入：`http://[IP位址]:8000`
   - 例如：`http://192.168.1.100:8000`

**未來多站版（v2.0）會支援多台電腦同步作業**

---

### Q5: 如何關閉系統？

**正確關閉步驟：**
1. 回到黑色視窗（Terminal / 命令提示字元）
2. 按鍵盤：**Ctrl + C**
3. 看到「已停止」訊息
4. 關閉視窗

⚠️ **不要直接關閉視窗！** 可能導致資料未儲存

---

### Q6: 系統更新後，舊資料還在嗎？

**更新步驟：**
1. 備份 `medical_inventory.db`
2. 下載新版本系統
3. 把舊的 `medical_inventory.db` 複製到新資料夾
4. 啟動新版本系統
5. 資料會自動升級

---

### Q7: 手機可以用嗎？

**可以！但有限制：**
- 手機需連接同一個 WiFi
- 開啟瀏覽器，輸入：`http://[電腦IP]:8000`
- 某些功能可能在小螢幕上不太方便

**建議使用平板或筆電以獲得最佳體驗**

---

## 技術資訊

> 以下內容供 IT 人員參考

### 系統需求

- **作業系統**: Windows 10+, macOS 10.14+, Linux
- **Python 版本**: 3.8 或更高
- **瀏覽器**: Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
- **記憶體**: 建議 4GB 以上
- **硬碟空間**: 100MB（不含資料庫）

### 技術架構

**後端 (Backend):**
- FastAPI 0.104.1
- SQLite3
- Uvicorn ASGI Server
- Python 3.11+

**前端 (Frontend):**
- Alpine.js v3.13.3
- Tailwind CSS v3.3.6
- Heroicons v2.0.18
- 響應式設計 (RWD)

### 資料庫 Schema

主要資料表：
- `items` - 物品主檔
- `inventory_events` - 庫存異動記錄
- `blood_inventory` - 血庫庫存
- `equipment` - 設備主檔（含 station_id 隔離）
- `surgery_records` - 手術記錄
- `dispense_records` - 藥品領用記錄

### API 端點

完整 API 文件：http://localhost:8000/docs

主要端點：
- `GET /api/items` - 查詢物品
- `POST /api/receive` - 進貨
- `POST /api/consume` - 消耗
- `GET /api/blood/inventory` - 血庫庫存
- `GET /api/equipment?station_id={id}` - 設備查詢（含站點過濾）
- `POST /api/dispense/normal` - 正常領藥
- `POST /api/dispense/emergency` - 緊急領藥

### 設定檔

**config/station_config.json:**
```json
{
  "station_id": "TC-01",
  "station_name": "醫療站 TC-01",
  "station_type": "health_center"
}
```

### 部署方式

**Docker 部署（推薦）：**
```bash
docker-compose up -d
```

**手動部署：**
```bash
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements_v1.4.5.txt
python3 main.py
```

**使用 systemd（Linux）：**
```bash
sudo cp mirs.service /etc/systemd/system/
sudo systemctl enable mirs
sudo systemctl start mirs
```

---

## 版本歷史

### v1.4.5 (2024-11-25)
- ✨ 新增多站點切換功能（TC/BORP/LOG）
- ✨ 自動範本載入（新站點自動複製設備清單）
- ✨ 手術器械命名標準化（TC-SURG-*, BORP-SURG-*, LOG-SURG-*）
- ✨ LOG-HUB 物資中心支援手術器械儲存
- 🐛 修復處置標籤頁搜尋功能
- 🐛 修復 dispense_records 資料表結構
- 🐛 修復設備過濾邏輯

### v1.4.0 (2024-11-20)
- ✨ 藥品領用系統（MIRS v2.3）
- ✨ 緊急領用 Break-the-Glass 功能
- ✨ 血庫病患資訊記錄
- 🎨 UI 全面重構（Heroicons + 新色系）

### v1.3.0 (2024-11-15)
- ✨ 設備每日自動重置（07:00am）
- ✨ 手術記錄與一般消耗整合
- 🎨 響應式設計優化

---

## 支援與聯絡

### 技術支援

- **GitHub Issues**: https://github.com/cutemo0953/medical-inventory-system_v1.4.5/issues
- **Email**: tom@denovortho.com

### 開發團隊

- **De Novo Orthopedics Inc.**
- **壯闊台灣** 醫療站支援計畫

---

## 授權資訊

© 2024 De Novo Orthopedics Inc. All rights reserved.

本系統專為壯闊台灣醫療站、備援手術站及物資中心開發。

---

## 附錄：快速指令參考

### Windows 用戶

```batch
# 啟動系統
cd Desktop\medical-inventory-system_v1.4.5
python main.py

# 檢查 Python 版本
python --version

# 安裝套件
pip install -r requirements_v1.4.5.txt

# 查看 IP 位址
ipconfig
```

### Mac/Linux 用戶

```bash
# 啟動系統
cd ~/Downloads/medical-inventory-system_v1.4.5
python3 main.py

# 檢查 Python 版本
python3 --version

# 安裝套件
pip3 install -r requirements_v1.4.5.txt

# 查看 IP 位址
ifconfig | grep inet
```

---

**醫療站庫存管理系統 v1.4.5**
*讓醫療物資管理更簡單*

🏥 Powered by De Novo Orthopedics Inc.
🤝 與壯闊台灣共同開發
